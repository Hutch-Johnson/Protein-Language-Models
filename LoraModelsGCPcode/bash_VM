sudo apt-get update
sudo apt-get install -y python3-pip

pip install transformers peft accelerate google-cloud-storage sentencepiece einops torch --break-system-packages

sudo apt-get install -y tmux

tmux new -s progen2

pip install transformers peft accelerate google-cloud-storage sentencepiece einops torch --break-system-packages

python3 progen2_lora.py --batch_size 12 --num_workers 1 --total_domains 12
```